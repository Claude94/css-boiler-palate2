//animate 라이브러리 설정 적용
$('.animate').scrolla({
    mobile: false,
    once: false
});

//slick 라이브러리 설정 적용
$('section.visual .slide').slick({
    autoplay: true,
    autoplaySpeed: 1000,
    fade: true
});